const bruh = {
    onClick: function() {
        return d ? p() : 0 === c.credits.premium ? c.hasActiveSubscription ? l((0,
            w.jsx)(b, {
                currentUser: c
            })) : l((0,
        w.jsx)(k, {})) : l((0,
        w.jsx)(v, {
            currentUser: c,
            startChallenge: p
        }))
    },
}